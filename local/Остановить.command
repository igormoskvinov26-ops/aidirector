#!/usr/bin/env bash
# Двойной щелчок по этому файлу останавливает Директора на Mac.
# Из Терминала — ./stop.sh, без ожидания Enter в конце.
cd "$(dirname "$0")"
./stop.sh
echo ""
read -r -p "Нажмите Enter, окно можно закрыть..." _
